import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabStyles: {
    paddingBottom: '5%', 
    paddingTop: 15
  },

  main: {
    borderTopWidth: 0,
    height: 60,
    elevation: 0,
    shadowOpacity: 0
  }
});

export default styles;