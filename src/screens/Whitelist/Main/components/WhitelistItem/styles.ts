import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  initialMargin: {
    marginTop: '10%'
  },
  container: {
    height: 50,
    flexDirection: 'row',
    marginHorizontal: '5%',
    marginVertical: '4%'
  }
});

export default styles;
