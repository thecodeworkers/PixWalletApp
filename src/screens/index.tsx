import React from 'react';
import Main from './Main';
import { NavigationContainer } from '@react-navigation/native';

const Root = () => (
  <NavigationContainer>
    <Main/>
  </NavigationContainer>
);

export default Root;
