export const actionObject = (type: string, payload: any = null) => {
  return { type, payload }
}

// export const orderArray = (array: any) => {
//   data.gradients[0] != data.gradients[1] ? newArray = data.gradients.reverse() : data = data.gradients;

// }
