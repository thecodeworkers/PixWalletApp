import { DefaultProps } from '../../types';

export type GeneralProps = {
  currency: any,
  action: any,
  gradient: boolean,
} & DefaultProps;

export type ReducersProps = {
  currency: any,
} & DefaultProps;
