import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  infoContainer:{
    flexDirection: 'row',
    borderRadius: 6,
    width:'70%',
    alignSelf: 'center',
    padding:10
  },
  infoIcon:{
    padding: 10,
    margin: 5,
    
  },
  infoText:{
    fontSize: 14,
   
    paddingTop:10,
    paddingBottom:10
  }

});

export default styles;
