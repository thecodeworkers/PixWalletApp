import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  touchable: {
    width: '100%',
    alignItems: 'center',
    alignSelf:'center',

  },
  gradientButton: {
    padding: '4.5%',
    alignItems: 'center',
    width: '100%',
    borderRadius:5,
  },
  text:{
    fontSize:16,
    fontWeight:'bold'
  }
});

export default styles;
