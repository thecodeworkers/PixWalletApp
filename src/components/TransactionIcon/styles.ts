import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  leftArrow: {
    transform:[{
      rotate: '25deg'
    }]
  },
  rightArrow: {
    transform: [{
      rotate: '-25deg'
    }]
  }
});

export default styles;
